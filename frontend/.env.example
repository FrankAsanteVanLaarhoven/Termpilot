# Leave NEXT_PUBLIC_API_BASE unset in production so the browser uses same-origin /api.
# Local `npm run dev` rewrites /api to the FastAPI process below.
API_BASE=http://127.0.0.1:8000
# NEXT_PUBLIC_API_BASE=
